package com.java.accessModifiers.privatePack;

public class PrivateMembers {
	// If a member is private then we can access that member only within the class i.e. we can't access private member
	// from outside of class.

	// Abstract methods should be available to the child classes to provide implementation where as private methods are
	// not available to the child classes. Hence, private abstract combination is illegal for methods.

}
